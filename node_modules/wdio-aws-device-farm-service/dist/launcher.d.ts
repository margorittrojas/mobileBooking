import { Services, Capabilities, Options } from "@wdio/types";
interface DeviceFarmConfig extends Services.ServiceOption {
    projectArn: string;
    expiresInSeconds?: number;
}
export default class DeviceFarmLauncher implements Services.ServiceInstance {
    private readonly _options;
    private readonly devicefarm;
    constructor(_options: DeviceFarmConfig);
    onPrepare(_config: Options.Testrunner, capabilities: Capabilities.RemoteCapabilities): Promise<void>;
    private setCapabilitySession;
    private createSession;
}
export {};
//# sourceMappingURL=launcher.d.ts.map